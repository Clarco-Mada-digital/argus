export function formatDate(d) { return new Date(d).toISOString(); }
export function unusedHelper(x) { return x * 2; }
export function jamaisUtilisee(a) {
  if (a > 0) { if (a > 1) { if (a > 2) { if (a > 3) { if (a > 4) { return a; } } } } }
  return 0;
}
